namespace Destination.Infrastructure.DomainEvents;

using SharedKernel;

public interface IDomainEventsDispatcher
{
    Task DispatchAsync(IEnumerable<IDomainEvent> domainEvents, CancellationToken cancellationToken = default);
}
